require 'spec_helper'

describe Markdown::Parser do
  it 'has a version number' do
    expect(Markdown::Parser::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
